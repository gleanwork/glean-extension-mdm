{
  "name": "glean",
  "displayName": "Glean",
  "description": "Connect Cursor, Windsurf, and Antigravity to your company's knowledge with Glean",
  "version": "0.4.0",
  "publisher": "glean",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/gleanwork/glean-extension-mdm"
  },
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "MCP"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Glean",
      "properties": {
        "glean.mcpServerUrl": {
          "type": "string",
          "default": "",
          "description": "MCP server URL for Glean. When set, this takes priority over system and user config files."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --out glean.vsix",
    "test": "vitest run",
    "test:e2e": "tsx e2e/run.ts",
    "test:smoke": "bash e2e/smoke-test.sh"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/vscode": "^1.93.0",
    "@vscode/vsce": "^3.2.2",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
